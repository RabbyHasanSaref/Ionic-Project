import { IonFooter, IonToolbar, IonTitle } from '@ionic/react';

const Footer = () => (
  <IonFooter>
    <IonToolbar>
      <IonTitle>© 2025 My App</IonTitle>
    </IonToolbar>
  </IonFooter>
);

export default Footer;
