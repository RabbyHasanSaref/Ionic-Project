import { IonToolbar, IonTitle, IonHeader } from '@ionic/react';

const Header = () => (
  <IonHeader>
    <IonToolbar>
      <IonTitle>My App</IonTitle>
    </IonToolbar>
  </IonHeader>
);

export default Header;
